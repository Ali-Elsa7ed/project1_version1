# HAYARCO - نظام الإدارة المتقدم

نظام إدارة متقدم لتتبع المصروفات والحضور مع دعم متعدد اللغات (عربي/إنجليزي).

## 📁 بنية المشروع

```
جعفر العمدة 2/
├── src/
│   ├── locales/           # ملفات الترجمة
│   │   ├── ar.js         # الترجمة العربية
│   │   ├── en.js         # الترجمة الإنجليزية
│   │   └── index.js      # تصدير الترجمات
│   ├── data/             # البيانات الأولية
│   │   └── initialData.js # البيانات الافتراضية
│   ├── App.jsx           # المكون الرئيسي
│   ├── index.css         # الأنماط العامة
│   └── main.jsx          # نقطة الدخول
├── public/               # الملفات العامة
├── index.html           # صفحة HTML الرئيسية
├── package.json         # تبعيات المشروع
└── vite.config.js       # إعدادات Vite
```

## 🌍 نظام الترجمة

### إضافة لغة جديدة:

1. أنشئ ملف جديد في `src/locales/` (مثل: `fr.js` للفرنسية)
2. انسخ بنية الترجمة من `ar.js` أو `en.js`
3. أضف الترجمات الخاصة بك
4. قم بتصدير اللغة الجديدة في `src/locales/index.js`

مثال:
```javascript
// src/locales/fr.js
export const fr = {
    companyName: 'HAYARCO',
    tagline: 'Système de gestion avancé',
    // ... باقي الترجمات
};

// src/locales/index.js
import { ar } from './ar';
import { en } from './en';
import { fr } from './fr';

export const translations = {
    ar,
    en,
    fr
};
```

## 📊 البيانات الأولية

يمكنك تعديل البيانات الافتراضية في `src/data/initialData.js`:

- **INITIAL_USERS**: المستخدمين الافتراضيين (مدير/موظف)
- **INITIAL_EXPENSES**: المصروفات الافتراضية
- **INITIAL_DEPTS**: الأقسام الافتراضية
- **COLORS**: ألوان النظام

## 🚀 التشغيل

### تثبيت التبعيات:
```bash
npm install
```

### تشغيل المشروع:
```bash
npm run dev
```

### بناء المشروع للإنتاج:
```bash
npm run build
```

## 🔐 بيانات الدخول الافتراضية

### المدير:
- البريد: `admin@system.com`
- كلمة المرور: `admin123`

### الموظف:
- البريد: `user@system.com`
- كلمة المرور: `user123`

## ✨ المميزات

- ✅ دعم متعدد اللغات (عربي/إنجليزي)
- ✅ إدارة المصروفات
- ✅ تتبع الحضور والانصراف
- ✅ نظام الإشعارات
- ✅ تصدير التقارير (Excel)
- ✅ تحديد الموقع الجغرافي (GPS)
- ✅ واجهة مستخدم حديثة ومتجاوبة
- ✅ حفظ البيانات محلياً (LocalStorage)

## 🛠️ التقنيات المستخدمة

- **React 18** - مكتبة واجهة المستخدم
- **Vite** - أداة البناء
- **Lucide React** - الأيقونات
- **CSS3** - التنسيق والأنماط

## 📝 ملاحظات

- جميع البيانات يتم حفظها محلياً في المتصفح (LocalStorage)
- يمكن تبديل اللغة في أي وقت من الزر العلوي
- النظام يدعم RTL للعربية و LTR للإنجليزية

---

Made with ❤️ by HAYARCO Team
